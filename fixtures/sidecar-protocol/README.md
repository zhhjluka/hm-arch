# Sidecar protocol golden fixtures

Shared JSON objects for Python and TypeScript contract tests. Each pair is named
`<sequence>-<operation>-<direction>.json`. `full-session.jsonl` is an ordered
JSONL transcript (request line, response line, …).

Do not embed host-agent-specific fields in these fixtures.
