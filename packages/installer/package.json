{
  "name": "@hm-arch/installer",
  "version": "2.0.5",
  "description": "npm installer and launcher for HM-Arch agent memory integrations",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "hm-arch-install": "dist/cli.js"
  },
  "files": [
    "dist",
    "scripts/postinstall.mjs"
  ],
  "scripts": {
    "prepare": "npm run build",
    "build": "node ./scripts/sync-bundled-version.mjs && tsc -p tsconfig.json && cp src/bundled-version.json dist/bundled-version.json && cp src/installer-version.json dist/installer-version.json",
    "test": "npm run build && node ./scripts/run-tests.mjs",
    "postinstall": "node ./scripts/postinstall.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
