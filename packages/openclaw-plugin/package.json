{
  "name": "@hm-arch/openclaw-plugin",
  "version": "0.1.0",
  "description": "HM-Arch memory plugin for OpenClaw with persistent Python sidecar",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "openclaw.plugin.json",
    "index.mjs"
  ],
  "exports": {
    ".": "./dist/index.js"
  },
  "openclaw": {
    "extensions": ["./index.mjs"]
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tsc -p tsconfig.test.json && node ./scripts/run-tests.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  }
}
